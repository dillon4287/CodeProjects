# import numpy as np
from math import*


# def importance(draws):
#     h = np.random.randn(draws,1)
#     smallTails = np.random.randn(draws,1) * 1.5 + 3
#     fatTails = np.random.standard_t(3, size=(draws,1)) * 1.5 + 3
#     weights = np.divide(smallTails, fatTails)
#     # print np.matmul(np.transpose(h), weights)
#
# a = [[1, 0], [0, 1]]
# b = [[4, 1], [2, 2]]
#
# print(np.matmul(a, b))


# importance(100)